# ANIME App

React practice project for routing, list rendering, reusable cards, and navigation components.

## Run

```bash
npm install
npm start
```

## Structure

- `public/`: static HTML entry file.
- `src/book/`: feature-specific book and character UI.
- `src/shared/`: reusable shared components, hooks, and navigation elements.
